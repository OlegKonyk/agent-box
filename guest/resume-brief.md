# Resumed run, after run {PARENT} stopped to ask

Run {PARENT} ended `{STATE}` on branch `{BRANCH}`. The work tree is exactly as
it left it. It asked the operator this:

> {ASK}

The operator answered:

> {ANSWER}

Take that answer as decided. Do not re-open the question. Continue the
original brief from where the previous run stopped; do not redo work that is
already committed on this branch. If the answer changes what "done" means,
say so in your final report.

The original brief follows.

---

{BRIEF}
